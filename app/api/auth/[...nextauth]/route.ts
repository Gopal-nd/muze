import { authOptions } from "@/utils/auth";
import NextAuth from "next-auth/next";

const hadldler:any = authOptions
const handler = NextAuth(
    //auth options
hadldler
    
)

export {handler as GET, handler as POST}

